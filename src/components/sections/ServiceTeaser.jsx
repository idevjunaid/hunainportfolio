import React from 'react';
import { Box, Container, Typography, Grid } from '@mui/material';

const ServiceTeaser = () => {
  return (
    <Box
      component="section"
      sx={{
        py: 8,
        bgcolor: 'background.paper',
        color: 'text.primary',
      }}
    >
      <Container>
        {/* Service teaser content will go here */}
      </Container>
    </Box>
  );
};

export default ServiceTeaser;
